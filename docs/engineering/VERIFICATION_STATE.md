# SYRAVEN — Verification State

Every row is a result that actually ran, with the tree it ran on. A check
that has not run on the current tree says **NOT RUN**. Never copy a
result forward to a tree it was not run on.

**Tree notation:** `6de7dd0+WT` = HEAD `6de7dd0` plus the uncommitted
working tree described in `PROJECT_STATE.md` at the stated moment.

| Check | Command | Result | When | Tree | Notes |
|---|---|---|---|---|---|
| Typecheck | `npx tsc --noEmit -p tsconfig.json` | **PASS** (exit 0) | 2026-09-13, end of Phase 1 | 6de7dd0+WT (Phase 1 final) | Re-run for the control plane: see last row |
| Lint — changed files | `npx eslint <files>` | **PASS** (exit 0) | 2026-09-13 | Phase 1 files | One pre-existing unused type in `/api/action` fixed |
| Lint — whole repo | `npm run lint` | **NOT RUN** | — | — | Heavy on this machine; not needed for any change so far |
| Unit + security suite | `npm run test:lowmem` | **PASS** — 1759 tests: 1757 pass, 0 fail, 2 todo | 2026-09-13, end of Phase 1 | 6de7dd0+WT (Phase 1 final) | The 2 todo are recorded BLOCKED/PARTIAL invariants |
| Architecture invariants | `npm run test:invariants` (then: single file) | **PASS** — 36 tests: 34 pass, 2 todo | 2026-09-13 | Phase 1 final | Included in the suite above |
| Mutation — Phase 1 invariants | scratch script (not in repo) | **15/15 killed**, files restored | 2026-09-13 | Phase 1 final | Depends on HEAD predating the fixes; see `VERIFICATION_COMMANDS.md` |
| Mutation — Brain bridge suite | scratch script (not in repo) | **NOT RUN** | — | — | Written, never executed |
| Mutation — earlier committed work | — | Reported in earlier sessions; **not re-run** on this tree | — | ≤ 6de7dd0 | — |
| Production build | `npm run build` | **NOT RUN** on current tree | — | — | Last success: BUILD_ID `8s0c9m4RUNW1nDCSDTjtQ`, built for the **test** project from `6de7dd0` (before the uncommitted work) |
| E2E — seeded journey | `npx playwright test tests/e2e/seeded-journey.spec.ts --project=chromium-desktop` | **PARTIAL** — 10/12 pass; 2 failures were fixture defects (fixed, not browser-re-verified) | 2026-09-13 | build of `6de7dd0`, spec from WT | Rerun **BLOCKED**: server → Supabase auth `AuthRetryableFetchError` status 0 under memory pressure. Teardown verified: 0 tagged rows |
| E2E — public pages, authenticated | `npm run test:e2e` | **NOT RUN** this session | — | — | — |
| Live semantic probe | scratch script, test project, real provider | **PASS** — right passage ranked first (0.542 vs 0.217); cleanup 0 rows | 2026-09-13 | WT | Provider: text-embedding-3-small |
| Security verification | security suites in the unit run | **PASS** (static and fake-client) | 2026-09-13 | Phase 1 final | No live RLS/penetration test has been run: **NOT RUN** |
| Secret scan (tracked files) | `git grep -lE <key patterns>` | **PASS** — no key-shaped strings, no secret-named files | 2026-09-13 | HEAD | Untracked files are covered by the control-plane verification below |
| Typecheck (control plane) | `npx tsc --noEmit -p tsconfig.json` | **PASS** (exit 0) | 2026-09-13, after bootstrap | 6de7dd0+WT (control plane) | `playwright.config.ts` and a new test changed |
| Lint (control plane) | `npx eslint playwright.config.ts tests/security/engineering-control-plane.test.ts` | **PASS** (exit 0) | 2026-09-13 | 6de7dd0+WT (control plane) | — |
| Invariants + control-plane guard | `npm run test:invariants` | **PASS** — 47 tests: 45 pass, 0 fail, 2 todo | 2026-09-13 | 6de7dd0+WT (control plane) | Invariants suite unchanged; todos are the recorded BLOCKED/PARTIAL items |
| Mutation — control plane | scratch script (not in repo) | **12/12 killed**, files restored byte-for-byte | 2026-09-13 | 6de7dd0+WT (control plane) | Extra server, floating package, `--caps`, persistent profile, unblocked prod origin, unguarded test server, healer fixme/scope, planner unsafe code, ignore rule, prod guard, Phase 1 overstated |
| Playwright config + production guard | `npx playwright test --list` | **PASS** — config loads, guard passes, 78 tests in 3 files listed | 2026-09-13 | control plane | No browser, no build |
| Build target of `.next` | `assertBuildIsNotProduction()` | **PASS** — not production | 2026-09-13 | `.next` from `6de7dd0` | — |
| MCP registration / approval | `claude mcp list`; approval keys in `.claude/settings.local.json` | **BLOCKED — still PENDING APPROVAL** for `playwright` and `playwright-test`; no `enabledMcpjsonServers` / `enableAllProjectMcpServers` recorded; no Playwright tools in the verifying session | 2026-09-13, MCP verification | control plane | Approval is the founder's: accept the project servers when an interactive `claude` session starts in the repo (or via `/mcp`). Not recorded on the founder's behalf |
| MCP protocol handshake | scratch script: stdio `initialize` → `tools/list` for each `.mcp.json` server, no tool called | **PASS** — `playwright`: Playwright 1.63.0, protocol 2025-06-18, 24 tools. `playwright-test`: Playwright Test Runner 1.63.0, protocol 2025-06-18, 89 tools. 0 leftover processes | 2026-09-13 | control plane | Proves the servers start and speak MCP. It is **not** a Claude Code connection |
| MCP tool usability (a tool actually called) | — | **NOT RUN** | — | — | Needs approval first; also no browser was launched (≈200 MB free) |
| MCP production-origin protection | `.mcp.json` args + control-plane suite | **PASS** — `--blocked-origins https://wpmbumtpcuahyqmdeqgf.supabase.co`, `--isolated`, `--headless`, no `--caps` | 2026-09-13 | control plane | Config-level only: blocking was not exercised in a browser. Playwright documents it as not a security boundary |
| E2E build-target guard | `git diff HEAD -- tests/e2e/buildTarget.ts`; `assertBuildIsNotProduction()`; `e2e-build-target`, `e2e-production-guard`, `engineering-control-plane` suites | **PASS** — guard file identical to HEAD; config production guard unchanged (config diff is screenshot settings only); `.next` not production; suites 35/35 | 2026-09-13 | control plane | — |
| MCP tool surface — finding | handshake tool lists | **RESOLVED with residual risks** (was OPEN) — see the hardening rows below | 2026-09-13, MCP hardening | control plane | `MCP_AUDIT.md` rewritten from the handshake lists |
| MCP hardening — deny rules | `.claude/settings.json` `permissions.deny` (27 rules) | **APPLIED** — `playwright`: 24 exposed, 3 denied, 21 available. `playwright-test`: 89 exposed, 24 denied, 65 available | 2026-09-13 | control plane | Denied: `browser_run_code_unsafe` (both), `browser_drop` (both), `browser_file_upload` (`playwright`), cookie ×5, localStorage ×5, sessionStorage ×5, `browser_storage_state`, `browser_set_storage_state`, `browser_route`, `browser_route_list`, `browser_unroute`, `browser_start_tracing`, `browser_stop_tracing` |
| MCP hardening — rules vs real tools | scratch `mcp-perm-check.mjs` (handshake both servers, no tool called, no browser) | **PASS** — both servers STARTED; 27/27 deny rules match a real exposed tool; agent MCP tools exposed and not denied: planner 20/20, generator 19/19, healer 9/9; problems 0; 0 leftover processes | 2026-09-13 | control plane | Enforcement is Claude Code's; a direct handshake still lists every tool |
| MCP hardening — config validity | JSON parse; `claude mcp list` | **PASS** — `.mcp.json`, `.claude/settings.json`, `.claude/settings.local.json` parse; Claude Code lists both servers | 2026-09-13 | control plane | Both still **⏸ Pending approval** — approval is the founder's |
| MCP hardening — tests | `node --test engineering-control-plane e2e-build-target e2e-production-guard`; `npm run test:invariants` | **PASS** — 40/40; invariants + control plane 52: 50 pass, 0 fail, 2 todo | 2026-09-13 | control plane | New assertions: high-risk tools denied, rules name only audited servers, no agent tool denied, essentials available, settings grant/approve nothing |
| MCP hardening — mutation | scratch `mut-control-plane.mjs` | **18/18 killed**, files restored | 2026-09-13 | control plane | 6 new: cookie read re-allowed, unsafe code re-allowed, agent tool denied, screenshot denied, settings approve servers, rule for unknown server |
| MCP hardening — guards | `.mcp.json` args; `git diff HEAD -- tests/e2e/buildTarget.ts`; `assertBuildIsNotProduction()` | **PASS** — production origin still blocked, `--isolated`, `--headless`; `buildTarget.ts` identical to HEAD; `.next` not production | 2026-09-13 | control plane | — |
| MCP hardening — typecheck / lint | `npx tsc --noEmit -p tsconfig.json`; `npx eslint tests/security/engineering-control-plane.test.ts` | **PASS** — exit 0 / exit 0 | 2026-09-13 | control plane | — |
| MCP residual risks (unchanged by design) | — | **ACCEPTED, documented** — `browser_evaluate` (all agents; can read `document.cookie`/web storage), `browser_network_request` (planner, healer; headers may carry `Cookie`), `browser_file_upload` on `playwright-test` (planner, generator) | 2026-09-13 | control plane | Denying them would break the agent workflow; see `MCP_AUDIT.md` → residual risks |
| Secret scan (tracked + untracked) | `git grep` / `grep` over untracked files | **PASS** — none | 2026-09-13 | control plane | — |
| Full unit suite on control-plane tree | `npm run test:lowmem` | **NOT RUN** | — | — | Only setup-related verification was in scope |
| MCP approval + connection (interactive session) | `.claude/settings.local.json` `enabledMcpjsonServers`; Claude Code tool list | **PASS** — both servers approved. `playwright`: connected, 21 tools. `playwright-test`: first `CONNECT_TIMEOUT` (30000 ms), connected after `/mcp` reconnect, 65 tools | 2026-09-13, MCP verification | control plane | Supersedes the PENDING row above |
| MCP tool call — `playwright-test` | `browser_navigate("about:blank")` | **BLOCKED** — `Error: Must setup test before interacting with the page`; no browser launched | 2026-09-13 | control plane | Setup (`planner_setup_page`) runs the config's `webServer` (`npm run build && next start`); not run — memory and scope |
| **Tree notation for the rows below** | — | — | — | `6de7dd0+WT (P1 pass 2)` | Control-plane tree plus the second Phase 1 pass (`PROJECT_STATE.md`, effort 4) |
| Typecheck (P1 pass 2) | `npx tsc --noEmit -p tsconfig.json` (heap 1536 MB) | **PASS** (exit 0) | 2026-09-13 | 6de7dd0+WT (P1 pass 2) | 401 MB free at start |
| Lint — changed files (P1 pass 2) | `npx eslint` on `runSettlement.ts`, `orchestrator.ts`, `approvalStore.ts`, `agents/run/route.ts`, `canvas/route.ts`, `studio/page.tsx`, `run-settlement.test.ts`, `architecture-invariants.test.ts` | **PASS** (exit 0) | 2026-09-13 | 6de7dd0+WT (P1 pass 2) | — |
| Invariants + settlement (P1 pass 2) | `node --test architecture-invariants run-settlement` | **PASS** — 57: 55 pass, 0 fail, 2 todo | 2026-09-13 | 6de7dd0+WT (P1 pass 2) | Todos: I-14 scheduler (BLOCKED), I-15 outcome store (PLANNED) |
| Unit + security suite (P1 pass 2) | `npm run test:lowmem` | **PASS** — 1804: 1802 pass, 0 fail, 2 todo (37 s) | 2026-09-13 | 6de7dd0+WT (P1 pass 2) | +45 tests vs the first pass |
| Mutation — P1 pass 2 | scratch `mut-phase1.mjs` (not in repo) | **20/20 killed**, every file restored byte-for-byte | 2026-09-13 | 6de7dd0+WT (P1 pass 2) | First run 17 killed + 3 NOT-APPLICABLE (patterns were LF, files CRLF); CRLF-aware re-run 3/3. Gate, settlement, orchestrator wiring, both migrations, approval store, run route, canvas, studio |
| Production build (P1 pass 2) | `npm run build` | **NOT RUN — BLOCKED** | — | — | 554 MB free vs 1600 MB heap cap; last build 15+ min and once killed; a default build inlines `.env.local` (production) into `.next`. `.next` is unchanged (test target, `6de7dd0`) |
| Migrations `20260913120000`, `20260913130000` | — | **NOT RUN** against any database | — | — | No local Postgres. Checked only by the replay guards (text) |
| `requestApprovals` 42P10 finding | — | **NOT RUN** against a database | — | — | Evidence: Postgres ON CONFLICT inference rules + code. Fix is correct either way |
| Production RLS (`pg_policies`) | — | **NOT RUN** | — | — | Needs a production read — founder approval |
| E2E (P1 pass 2) | — | **NOT RUN** | — | — | No browser verification of the Studio or run-route changes |
| `messages` client write path (P1 step 2) | ripgrep over `app/`, `lib/`, `services/` (literal, split-line, variable-table helpers, `.rpc`); `git grep` at HEAD, `ccea043`, `ccea043^`; `git log -S/-G` over history; migrations for functions/triggers | **PASS** — no client insert into `messages` anywhere, now or in history; chat UI and `/api/chat`, `/api/stream` make no Supabase call; no function/trigger inserts into it | 2026-09-13 | 6de7dd0+WT (P1 step 2) | Justifies revoking INSERT |
| Migration 2 hardening (P1 step 2) | `20260913130000`: `revoke insert on table public.messages from authenticated;` + policy kept as defense in depth | **WRITTEN, NOT APPLIED** | 2026-09-13 | 6de7dd0+WT (P1 step 2) | Not executed against any database |
| Schema + security suites (P1 step 2) | `node --test "tests/schema/*.test.ts" "tests/security/*.test.ts"` | **PASS** — 1804: 1802 pass, 0 fail, 2 todo | 2026-09-13 | 6de7dd0+WT (P1 step 2) | Includes migration-integrity and the new revoke guard (I-2) |
| Mutation — messages revoke guard | scratch `mut-step2.mjs` | **3/3 killed**, file restored byte-for-byte | 2026-09-13 | 6de7dd0+WT (P1 step 2) | Revoke removed; INSERT re-granted; blanket re-grant |
| Typecheck (P1 step 2) | `npx tsc --noEmit -p tsconfig.json` (heap 1536 MB) | **PASS** (exit 0) | 2026-09-13 | 6de7dd0+WT (P1 step 2) | 420 MB free |
| Migration instructions (P1 step 3) | ripgrep for `db push` / `--include-all` over `*.md`, `docs/`, `supabase/`, `tests/`, `specs/`, `.claude/`, `package.json` | **PASS** — no runnable push command remains; every mention is a warning (`DATABASE.md` §1.1, §6; `MIGRATION_APPROVAL_REQUIRED.md`) | 2026-09-13 | 6de7dd0+WT (P1 step 3) | `DATABASE.md` §1.1 and §6 rewritten; no migration executed |
| Schema + control-plane + invariants (P1 step 3) | `node --test "tests/schema/*.test.ts" engineering-control-plane architecture-invariants` | **PASS** — 245: 243 pass, 0 fail, 2 todo | 2026-09-13 | 6de7dd0+WT (P1 step 3) | Documentation-only change; typecheck not needed |
| Supabase CLI target (P1 step 4) | read `supabase/.temp/project-ref`, `linked-project.json`; boolean checks on `pooler-url`; local `supabase --version`, `migration list/repair --help` | **LINKED TO PRODUCTION** — ref `wpmbumtpcuahyqmdeqgf` ("SYRAVEN AI"); pooler URL is production's, no password; CLI 2.116.0. `migration list/repair` accept `--project-ref` + `--password` or `--db-url` (targeting without re-link) | 2026-09-13 | 6de7dd0+WT (P1 step 4) | No network call to any database; link not changed |
| Test-project credentials (P1 step 4) | presence-only checks: env vars, `~/.supabase/access-token`, key names in `.env.local` / `.env.e2e.local`, ref match on `.env.local` `SUPABASE_DB_URL` (no value printed) | **BLOCKED** — no test-project DB password or DB URL anywhere; no access token in env or file; `.env.e2e.local` holds only URL, anon/publishable keys, test user. The only DB credential on the machine (`.env.local` `SUPABASE_DB_URL`) is **production's**, with password, direct host | 2026-09-13 | 6de7dd0+WT (P1 step 4) | `.env.local` / `.env.e2e.local` git-ignored and untracked |
| Test-project migration history | `supabase migration list` | **NOT RUN** | — | — | Needs the test DB password; the linked target is production, and reading its history is SQL against production |
| Config + E2E guards + schema (P1 step 4) | `node --test engineering-control-plane e2e-production-guard e2e-build-target "tests/schema/*.test.ts"` | **PASS** — 225: 225 pass, 0 fail | 2026-09-13 | 6de7dd0+WT (P1 step 4) | Read-only suites |
| **Test project — Phase 1 migrations: schema** | founder, manually in the test project's SQL Editor: `20260913120000`, `20260913130000` applied to `akhkukajdgayqwhedeoo` | **Schema applied and behavior verified on TEST; migration history not recorded.** | 2026-09-13 | test project | Production: **not applied** |
| **Test project — Phase 1 migrations: migration history** | founder, SQL Editor: `select version from supabase_migrations.schema_migrations where version in ('20260913120000','20260913130000')` | **NOT RECORDED** — no rows for either version. **Not** marked applied in history | 2026-09-13 | test project | `migration repair` not run: no test-project DB credential on this machine, and not authorized. A later `db push` against the test project would try to re-run both files; they are idempotent (`drop … if exists`, `revoke`, `grant`, `drop constraint if exists`), but the history must be recorded before any CLI use there |
| Test project — founder SQL Editor verification (P1 step 5 final) | founder, read-only catalog queries in the test project's SQL Editor | **PASS (founder-confirmed)** — `profiles` write privileges revoked; own SELECT remains; `messages` INSERT privilege revoked from `authenticated`; `projects` organization-membership enforcement active; `agent_approvals` column privileges match the migration; `agent_approvals_ttl_bounded` exists with the 16-minute definition; policies match the repository definitions | 2026-09-13 | test project | Query output seen by the founder, not by this session |
| Test project — privilege/policy probes (P1 step 5) | scratch `verify-test-migrations.mjs`, `verify-test-migrations-2.mjs`: PostgREST as the E2E test user (`authenticated`); target asserted = test ref; writes filtered to a nil id or built to fail (malformed uuid; NOT NULL + CHECK + FK after RLS) | **PASS (14 probes, 0 rows written)** — `profiles` UPDATE/DELETE/INSERT → 42501 *permission denied*; own SELECT → 200, 1 row. `messages` INSERT → 42501 *permission denied*. `projects` INSERT other owner → 42501 RLS; foreign organization → 42501 RLS. `agent_approvals` INSERT granted columns → 22P02 (granted, no row); + `created_at` / `decided_by_user_id` / `id` → 42501; UPDATE `expires_at` / `tool_id` / `created_at` → 42501; UPDATE `status, decided_by_user_id, decided_at` → 204 (granted, 0 rows) | 2026-09-13 | test project | Sign-in and sign-out created and revoked one auth session each run. REST schema description 401 (not available to `authenticated`), so gated probes were skipped in run 1 and re-designed in run 2 |
| **Production — policy/privilege reality read (P1 step 6)** | presence-only credential inventory: `.env.local` / `.env.e2e.local` key names, `SUPABASE_ACCESS_TOKEN`, `~/.supabase/access-token`, `psql`, CLI link | **BLOCKED — NOT RUN** — no read-only path to the production catalog exists on this machine. Available credentials are write-capable and excluded by instruction: `.env.local` `SUPABASE_DB_URL` (`postgres` role, password), `SUPABASE_SECRET_KEY` (service role; no catalog through REST either), CLI link → production. No read-only role, no access token, no `psql`. Nothing connected to production | 2026-09-13 | production | Safe route: founder runs the read-only query set (Phase 1 step 6 report) in the production SQL Editor inside `begin read only; … rollback;` |
| **Test project — `20260909120000` integration_connections (P1 step 6)** | (a) this session: anon `GET /rest/v1/integration_connections?limit=0` → 404 `PGRST205`, with controls (nonexistent table → `PGRST205`; `jobs` → 200); (b) founder, SQL Editor: `select to_regclass('public.integration_connections')` | **NOT APPLIED on TEST** — `to_regclass` = `NULL`; table, 17 columns, 4 policies, constraints, indexes and grants all absent. History: **not recorded** | 2026-09-13 | test project | Consistent with `MIGRATION_APPROVAL_REQUIRED.md`: awaiting approval |
| **Test project — `20260910120000` jobs lease policy (P1 step 6)** | founder, SQL Editor: `pg_policies` for `public.jobs`; `information_schema.column_privileges` (UPDATE, `authenticated`) | **NOT APPLIED on TEST** — `jobs` has only `users can create own jobs` (INSERT) and `users can view organization jobs` (SELECT); `users can cancel own jobs` absent; `authenticated` UPDATE covers **all** `jobs` columns, not only `status, updated_at`. History: **not recorded** | 2026-09-13 | test project | `authenticated` holds UPDATE on every `jobs` column (from `20260906120000`); with no UPDATE policy RLS still denies every update. Consistent with `MIGRATION_APPROVAL_REQUIRED.md`: awaiting approval |
| **Test project — migration history vs schema (P1 step 6)** | founder, SQL Editor: `select version from supabase_migrations.schema_migrations where version >= '20260908000000'` | **DIVERGENT** — recorded: `20260908120000` only. Not recorded: `20260909120000`, `20260910120000` (not applied) and `20260913120000`, `20260913130000` (schema applied manually, verified). A CLI `db push` against the test project would therefore apply `20260909120000` and `20260910120000` — both **unapproved** — and re-run the two Phase 1 files | 2026-09-13 | test project | Do not run any CLI push against the test project until the history is reconciled with founder approval and a test-project credential |
| Test project — anon SELECT on `jobs` (observation) | this session: anon `GET /rest/v1/jobs?select=id&limit=0` | **OBSERVED** — HTTP 200, 0 rows: `anon` holds SELECT on `public.jobs`; RLS returns nothing | 2026-09-13 | test project | Recorded only; not investigated, not changed. `20260906120000` names only `authenticated`/`service_role`, so the grant comes from elsewhere (platform defaults or drift) |
| **Tree notation for the step 7 rows** | — | — | — | `6de7dd0+WT (P1 step 7)` | Adds: `next.config.ts` headers, `/api/canvas` on the adapter, `security-headers.test.ts`, invariant guards + deferred `todo`s, doc corrections |
| Targeted suites (P1 step 7) | `node --test security-headers architecture-invariants usage-enforcement api-auth-boundary middleware-gate canvas-persistence run-settlement` | **PASS** — 354: 347 pass, 0 fail, 7 todo | 2026-09-14 | 6de7dd0+WT (P1 step 7) | 7 todo = I-14, I-15 + 5 deferred boundaries (jobs insert, org ownership/roles, latent client-writable tables, approval arguments, low/medium de-duplication) |
| Lint — touched files (P1 step 7) | `npx eslint next.config.ts app/api/canvas/route.ts tests/security/security-headers.test.ts tests/security/architecture-invariants.test.ts` | **PASS** (exit 0) | 2026-09-14 | 6de7dd0+WT (P1 step 7) | — |
| Mutation (P1 step 7) | scratch `mut-step7.mjs` | **10/10 killed**, files restored byte-for-byte | 2026-09-14 | 6de7dd0+WT (P1 step 7) | 7 header weakenings; OpenAI SDK restored in `/api/canvas`; latent table read as input; route trusting an org role |
| Typecheck (P1 step 7) | `npx tsc --noEmit -p tsconfig.json` (heap 1536 MB) | **PASS** (exit 0) | 2026-09-14 | 6de7dd0+WT (P1 step 7) | 452 MB free |
| Security headers in a browser / on a deployed response | — | **NOT RUN** | — | — | Verified against the config (`headers()` output), not on an HTTP response: no build, no browser |
| Unit + security suite (P1 step 7) | `npm run test:lowmem` | **PASS** — 1817: 1810 pass, 0 fail, 7 todo (139 s) | 2026-09-14 | 6de7dd0+WT (P1 step 7) | Includes the control-plane checks on the edited state documents. +13 tests vs pass 2 (1804) |
| **PHASE 1 FINAL GATE (step 8) — tree** | — | — | 2026-09-14 | `6de7dd0+WT (P1 final gate)` | Same tree as step 7; only state documents changed in step 8 |
| Repository integrity (final gate) | `git status`; `git ls-files --others --exclude-standard`; artifact paths; `git grep` for mutation residue | **PASS** — HEAD `6de7dd0` (`main`); working tree = the documented uncommitted efforts; no `.orig/.rej/.bak`; no mutation residue (`__mutant`, `x-invalid`, probe table names) in `app/ lib/ services/ supabase/`; `test-results/`, `.next/trace` present but git-ignored | 2026-09-14 | final gate | — |
| Secret scan (final gate) | secret-shaped patterns (Stripe live, `sb_secret_`, JWT, credentialed `postgres://`, private keys, AWS, GitHub, Slack, OpenAI `sk-`, `whsec_`) over tracked and untracked non-ignored files; names only | **PASS** — none. `.env.local`, `.env.e2e.local` ignored and untracked; `supabase/.temp` untracked | 2026-09-14 | final gate | — |
| Unit + security suite (final gate) | `npm run test:lowmem` | **PASS** — 1817: 1810 pass, 0 fail, 7 todo (111 s) | 2026-09-14 | final gate | 7 todo: I-14, I-15, 5 deferred boundaries |
| Invariants + control plane (final gate) | `npm run test:invariants` | **PASS** — 68: 61 pass, 0 fail, 7 todo | 2026-09-14 | final gate | — |
| Migration integrity (final gate) | `node --test "tests/schema/*.test.ts"` | **PASS** — 185: 185 pass | 2026-09-14 | final gate | — |
| E2E production guards (final gate) | `node --test e2e-production-guard e2e-build-target` | **PASS** — 24: 24 pass | 2026-09-14 | final gate | — |
| Playwright config + guard, no browser (final gate) | `npx playwright test --list` | **PASS** — config loads, production guard passes, 78 tests in 3 files | 2026-09-14 | final gate | — |
| `.next` build target (final gate) | `assertBuildIsNotProduction()` | **PASS** — not production; BUILD_ID `8s0c9m4RUNW1nDCSDTjtQ` (2026-09-13 08:27), **stale**: built from `6de7dd0`, not the current tree | 2026-09-14 | `.next` | — |
| Typecheck (final gate) | `npx tsc --noEmit -p tsconfig.json` (heap 1536 MB) | **PASS** (exit 0) | 2026-09-14 | final gate | First attempt NOT RUN at 139 MB free; waited, ran at 435 MB |
| Lint (final gate) | `npx eslint` on the 33 `.ts/.tsx` files the working tree changes or adds | **PASS** (exit 0) | 2026-09-14 | final gate | Whole-repo `npm run lint`: **NOT RUN** — `RESOURCE_POLICY.md` rule 5 (lint what changed) |
| Mutation (final gate) | — | **NOT RE-RUN** — no guard changed in step 8. Cumulative on this tree: pass 2 20/20, step 2 3/3, step 7 10/10 killed | — | final gate | Rows above |
| **Production build (final gate)** | `npm run build` | **NOT RUN — BLOCKED (resources + secret isolation)** | 2026-09-14 | — | Free memory 139–461 MB vs the 1600 MB heap cap (last build 15+ min, once killed). `next build` loads `.env.local` automatically, which holds production `SUPABASE_SECRET_KEY` and `SUPABASE_DB_URL`; the instruction forbids using production secrets, and excluding them would mean moving the secret file |
| **Browser / E2E (final gate)** | — | **NOT RUN — BLOCKED** | — | — | No build of the current tree exists (the only `.next` is stale, from `6de7dd0`); running E2E against it would verify old code. No browser launched |
| **PRODUCTION — `20260913120000` profiles billing lockdown** | founder, explicitly applied and verified in the production SQL Editor (`wpmbumtpcuahyqmdeqgf`) | **APPLIED + VERIFIED (founder-reported)** — `authenticated` on `profiles`: INSERT = false, UPDATE = false, DELETE = false; `profiles` has only SELECT policies | 2026-09-14 | production | Query output seen by the founder, not by this session. Not reported: `profiles.plan` ↔ Stripe reconciliation of rows possibly edited through the hole before the lockdown |
| **PRODUCTION — `20260913130000` tenant write boundaries** | founder, explicitly applied and verified in the production SQL Editor | **APPLIED + VERIFIED (founder-reported)** — `authenticated` on `messages`: INSERT = false; `agent_approvals` column privileges restricted to exactly the intended INSERT/UPDATE columns; `agent_approvals_ttl_bounded` present, `expires_at <= created_at + interval '16 minutes'`, NOT VALID by design | 2026-09-14 | production | Query output seen by the founder. **Not reported:** the `projects` INSERT/UPDATE policies (organization membership) in production; `messages` policies other than INSERT; the `agent_approvals_update_own` policy text |
| **PRODUCTION — migration history for both versions** | — | **NOT REPORTED** | — | production | If applied through the SQL Editor, `supabase_migrations.schema_migrations` may not record them. A `db push` against production would then re-run both (idempotent) **and apply the unapproved `20260909120000`, `20260910120000`** — never push from this checkout (`DATABASE.md` §6) |
| No production migration executed by this session | — | **CONFIRMED** — every production change was made by the founder; this session never connected to production | 2026-09-14 | — | — |
| Repository reconciliation after production apply | `git log -1`; `git status` (code paths); residue grep; `npm run test:lowmem` | **PASS** — HEAD `6de7dd0`; code unchanged since the final gate; no mutation residue; 1817: 1810 pass, 0 fail, 7 todo (79 s) | 2026-09-14 | `6de7dd0+WT (P1 final gate)` | — |
| Typecheck (reconciliation run) | `npx tsc --noEmit -p tsconfig.json` | **NOT RUN** this run (235 MB free, below 300 MB) — the code is identical to the final-gate tree, where it **PASSED** (exit 0) | 2026-09-14 | final gate | Cited, not re-run |
| **PRODUCTION — `projects` boundary (I-2)** | founder, production SQL Editor (`wpmbumtpcuahyqmdeqgf`): `pg_policies` for `public.projects` | **VERIFIED (founder-reported)** — INSERT `users can create projects` and UPDATE `owners and admins can update projects`; both WITH CHECK clauses enforce `organization_id IS NULL OR public.is_organization_member(organization_id)`. Re-confirmed in the same report: `messages` INSERT = false; `profiles` INSERT/UPDATE/DELETE = false; `agent_approvals` column privileges; `agent_approvals_ttl_bounded` | 2026-09-14 | production | Query output seen by the founder, not by this session. With it, I-2 is PASS |
| **Verification build — assessment** | read-only: `ci.yml`; `lib/supabaseAdmin.ts`, `lib/supabase.ts`; Next docs `environment-variables.md`; `git remote -v` | **BLOCKED — the only remaining Phase 1 gate** | 2026-09-14 | — | A secret-free build is known to work (`ci.yml`: "`next build` completes with a completely empty environment"; admin client is lazy). Local blocker: `next build` loads `.env.local` (production secrets) unless excluded, and 139–461 MB free vs a 1600 MB heap. CI (GitHub, `ubuntu-latest`, no `secrets:`, `contents: read`) runs `npm run build` only on push / PR to `main` — needs a founder-authorized commit and push, and confirmation that no Vercel Git integration deploys branch pushes (not visible from the repository) |
| Vercel Git integration | founder confirmation | **CONNECTED** to `canalikaraca62-oss/nova-ai` — a push or PR deploys (preview; production on `main`). CI route not used | 2026-09-14 | — | Never push without founder approval |
| **Secret-free scratch copy (build input)** | `git ls-files --cached --others --exclude-standard` → copy to `scratchpad\b`; SHA-256 per file vs canonical; `.env*` / `supabase/.temp` / secret-pattern scan; `robocopy node_modules` | **PASS** — 357 files, **0 hash mismatches**, tree manifest SHA-256 `ca69cfe11327724796ed094383552bf996d82cbb0a96f95f76ad92f1085a7e16` (HEAD `6de7dd0` + working tree); **no `.env*` file**, no `supabase/.temp`, no secret-shaped string; `node_modules` 31,168 files / 2,734 dirs copied, 0 failed (robocopy exit 1 = copied) | 2026-09-14 | `6de7dd0+WT (P1 final gate)` | Canonical repository untouched (`git status` 47 entries before and after) |
| **PRODUCTION BUILD — Phase 1 tree** | in `scratchpad\b`: `node node_modules/next/dist/bin/next build`, `NODE_ENV` unset (Next assigns `production`), `NODE_OPTIONS=--max-old-space-size=1600`, `NEXT_TELEMETRY_DISABLED=1`, no secret-like env var, no `.env*` file | **PASS** — exit 0 in 573 s; Next.js 16.3.4 (Turbopack); "✓ Compiled successfully in 5.7min"; TypeScript finished (2.8 min); "✓ Generating static pages (47/47)"; `BUILD_ID` `R3WTT03LAIpTqm5j4I0tf`; server output contains **neither** Supabase ref (0 / 0 files) | 2026-09-14 | `6de7dd0+WT (P1 final gate)` | Free memory 322 MB at start, 409 MB at end. One warning, pre-existing and not Phase 1: the `middleware` file convention is deprecated in favour of `proxy`. Nothing pushed, deployed or connected to production; `NODE_ENV=test` not used |
| Browser / E2E of the Phase 1 tree | — | **NOT RUN** | — | — | Not a Phase 1 DoD item. Needs a build targeting the test project (E2E env) and a browser run under the resource policy |
| **PHASE 1 — accepted as PASS** | founder acceptance of the evidence above | **PASS** | 2026-09-14 | `6de7dd0+WT (P1 final gate)` | I-7 and I-15 PARTIAL, I-14 BLOCKED stay documented limitations (`PHASE_STATE.md`; `todo`s in the invariant suite). Control-plane guard re-pointed: PASS must name those three at their real status and keep Phase 2 NOT STARTED |
| Test project — remaining after the founder's verification | — | **NOT VERIFIED** — (1) both versions in `supabase_migrations.schema_migrations` (**not recorded**); (2) behavior of the `projects` UPDATE rule and of the `agent_approvals` no-revert rule under a real write (definitions verified, not exercised: exercising them mutates a row); (3) the TTL constraint under a real insert (definition verified, not exercised) | — | test project | Superseded the earlier catalog-access row: policies, column privileges and the TTL constraint are now founder-verified |
| **PHASE 2 — status** | founder instructions 2026-09-14 / 2026-09-15 | **IN PROGRESS** — started, stopped as PARTIAL, resumed; P2-A … P2-G committed locally (16 commits after `3314baa`, HEAD `e0ac56f`, never pushed); P2-P (preflight follow-up) implemented and verified, **uncommitted**; P2-H and the Phase 2 final gate **NOT RUN** | 2026-09-15 | `e0ac56f+WT (P2-P)` | Per-batch results for P2-A … P2-G: `PURIFICATION_EVIDENCE.md`. Control-plane guard re-pointed with the docs: Phase 2 `IN PROGRESS` (never PASS before its gate), Phase 3 `NOT STARTED` |
| **Tree notation for the P2-P rows** | — | — | — | `e0ac56f+WT (P2-P)` | HEAD `e0ac56f` plus the P2-P working tree: `/chat/[id]` and `/privacy/activity` deleted, `/teams` link, `api-auth-boundary` sweep hardening, P2-P guards, Phase 2 status docs + guard |
| Guard suites (P2-P) | `node --test --test-concurrency=1` 12 affected suites (`PURIFICATION_EVIDENCE.md` P2-P) | **PASS** — 474: 474 pass, 0 fail | 2026-09-15 | `e0ac56f+WT (P2-P)` | First launch refused at the 300 MB gate (285 MB) |
| Mutation (P2-P) | scratch `mut-pp.mjs` | **14/14 caught** + unfixed-sweep baseline **gap confirmed** (unwrapped POST passed the `e0ac56f` sweep, 133/0); every file restored by hash | 2026-09-15 | `e0ac56f+WT (P2-P)` | Orphan pages restored / re-linked / web storage re-added; 4 handler export shapes; classifier skip restored; Phase 2/3 status overstated |
| Invariants + control plane (P2-P) | `npm run test:invariants` | **PASS** — 68: 61 pass, 0 fail, 7 todo | 2026-09-15 | `e0ac56f+WT (P2-P)` | — |
| Typecheck (P2-P) | `npx next typegen`; `npx tsc --noEmit -p tsconfig.json` (heap 1536 MB) | **PASS** — typegen exit 0 (0 references to the deleted pages left); tsc exit 0 | 2026-09-15 | `e0ac56f+WT (P2-P)` | 382 MB free at start |
| Unit + security suite (P2-P) | `npm run test:lowmem` | **PASS** — 1860: 1853 pass, 0 fail, 7 todo (37.8 s) | 2026-09-15 | `e0ac56f+WT (P2-P)` | +15 vs G-B7 (1845), accounted name by name in `PURIFICATION_EVIDENCE.md` P2-P |
| Lint — changed files (P2-P) | `npx eslint app/teams/page.tsx` + 5 changed test files (heap 768 MB) | **PASS** (exit 0) | 2026-09-15 | `e0ac56f+WT (P2-P)` | Whole-repo lint **NOT RUN** (`RESOURCE_POLICY.md` rule 5) |
| **Untracked artifact `syraven-audit.zip`** | metadata only: `Get-Item` (size, mtime) | **INTENTIONALLY EXCLUDED** by founder instruction (2026-09-15) — 2,444,143 bytes, mtime `2026-09-14T17:41:03.66Z`, unchanged across every P2-P step; not opened, scanned, modified, moved, deleted, staged or git-ignored | 2026-09-15 | — | The Phase 2 final gate's repository-integrity and secret-scan checks exclude it **by name** and report it as an excluded untracked artifact; it is also excluded from every scratch build copy |
| Scratch build copy (P2-P) | `git ls-files --cached --others --exclude-standard` minus `syraven-audit.zip` and the 2 deleted pages → `scratchpad\b`; SHA-256 per file; `.env*` / `supabase/.temp` / secret-pattern scan; secret-named env vars removed | **PASS** — 309 files, 0 hash mismatches; no `.env*`, no `supabase/.temp`, zip not in copy; 1 secret-pattern match, the known synthetic fixture in `tests/security/observability-redaction.test.ts:380` (a `postgres` URL with placeholder credentials `user` / `pw` and host `host`, used to prove error messages are never logged; since `5f2832f`), allowed by exact file and value; 1 secret-named env var removed (a session token name, no project secret) | 2026-09-15 | `e0ac56f+WT (P2-P)` | First run refused to build on that fixture before the allowance was added (build **NOT STARTED**) |
| Production build — attempt 1 (P2-P) | in `scratchpad\b`: `node node_modules/next/dist/bin/next build`, heap 1600 MB, telemetry off, `NODE_ENV` unset | **FAIL (environment)** — exit 1 after 13 s: `Cannot find module 'next/dist/compiled/webpack/webpack-lib'`. The scratch `node_modules` (left from Phase 1 under `%TEMP%`) held 2 of 28 files in `next/dist/compiled/webpack`; canonical install complete | 2026-09-15 | `e0ac56f+WT (P2-P)` | 523 MB free at start. Fix: `robocopy /MIR` from the canonical `node_modules` (29,601 files: 14,266 copied, 854 extras removed, 0 failed); the build script now compares Next's compiled directories file for file before building |
| Production build — attempt 2 (P2-P) | same script | **NOT STARTED** — the copy's secret scan matched the fixture's literal credentialed URL, which the verification row above had quoted word for word; the scan refused, as designed | 2026-09-15 | `e0ac56f+WT (P2-P)` | Row reworded to describe the fixture without the literal; `node_modules` check PASS (28/28, 148/148, 123/123) |
| **Production build — attempt 3 (P2-P)** | same script and command; copy PASS (309 files, 0 mismatches, 0 secret matches beyond the allowed fixture, no `.env*`), `node_modules` PASS | **BLOCKED (resources)** — started at 513 MB free (gate ≥ 300 MB); Next.js 16.3.4 reached "Creating an optimized production build …" and the process was **killed by the host for low memory** during compilation. No `BUILD_ID`; no process left; canonical tree and `syraven-audit.zip` unchanged | 2026-09-15 12:17 | `e0ac56f+WT (P2-P)` | Not retried (founder instruction: record BLOCKED rather than force). The Phase 1 build took 573 s with 322–409 MB free; free memory is the binding constraint on this ~3.9 GB machine. Last successful build remains the Phase 1 tree (`R3WTT03LAIpTqm5j4I0tf`) |
